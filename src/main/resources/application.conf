env=local

op-rabbit {
  topic-exchange-name = "amq.topic"
  connection {
    virtual-host = "/"
    hosts = [""]
    username = ""
    password = ""
    port = 9001
    ui.port = 5655
    ssl = false
    timeout = 3s
  }
}